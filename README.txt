this program requires pywin32 and pyautogui, you can manually install both by

pip install pywin32
pip install pyautogui

typing this in cmd or just run the install.bat file included, and it will do the work for you